import s from './Sort.module.css'

type Props = {
  isOpen: boolean
  closeHandler: () => void
}

const Filter: React.FC<Props> = ({ isOpen, closeHandler }) => {
  return <div></div>
}

export default Filter
