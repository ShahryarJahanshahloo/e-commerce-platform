import s from './Footer.module.css'

const Footer: React.FC = props => {
  return (
    <div className={s.footer}>
      <div className={s.inner}>this is goin to be footer</div>
    </div>
  )
}

export default Footer
