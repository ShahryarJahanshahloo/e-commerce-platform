export type ApiCartItem = {
  storageItem: string
  quantity: number
  price: number
  productId: string
  productName: string
}

export type FormCartItem = {
  storageItem: string
  quantity: number
}
